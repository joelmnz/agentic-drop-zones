ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
# Claude Code CLI path (defaults to 'claude')
# If already installed locally, try:
# export PATH="$HOME/node_modules/.bin:$PATH"
# If installed locally, set to: /path/to/node_modules/.bin/claude
CLAUDE_CODE_PATH=claude
REPLICATE_API_TOKEN=
# OpenCode CLI path (defaults to 'opencode')
# If installed via npm: /path/to/node_modules/.bin/opencode
# If installed via brew: /opt/homebrew/bin/opencode
# If installed via curl script: ~/.opencode/bin/opencode
OPENCODE_CLI_PATH=opencode
